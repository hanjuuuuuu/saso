<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wudc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wudc">
        <activity android:name=".RecordclassActivity"></activity>
        <activity android:name=".RecordclubActivity" />
        <activity android:name=".RecordcontestActivity" />
        <activity android:name=".RecorddayActivity" />
        <activity android:name=".RecordActivity"/>
        <activity android:name=".SecondActivity" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".LoginActivity"/>
        <activity android:name=".ProfileActivity"/>
        <activity android:name=".Profile2Activity"/>
        <activity android:name=".SettingActivity"/>
        <activity android:name=".Logo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>